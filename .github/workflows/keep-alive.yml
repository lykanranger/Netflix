name: Keep Supabase Alive

on:
  schedule:
    # Runs every 3 days at midnight
    - cron: '0 0 */3 * *'
  workflow_dispatch: # Allows manual triggering

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase API
        run: |
          curl -I https://<your_new_project_id>.supabase.co/rest/v1/

